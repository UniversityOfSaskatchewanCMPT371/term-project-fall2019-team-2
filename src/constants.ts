/**
 * Static readonly constants for magic numbers in the project
 */
export class CONSTANTS {
  /**
   * The target for the SVG element (where D3 gets drawn)
   */
  public static readonly SVG_SELECTOR = '#svgtarget';
}

export default CONSTANTS;
